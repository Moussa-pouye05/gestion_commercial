# TODO - Fix bugs in gestion_client.js

## Tasks:
- [x] Fix `isClientPage` variable to only check for relevant elements
- [x] Fix syntax error in the `DOMContentLoaded` event listener  
- [x] Fix error message text from "vendeur" to "client"
- [x] Fix `loadVendeur(currentPage)` to `loadClient(currentPageClient)` in createClient function
